import { Layout } from "@/components/Layout";
import ReturnsContainer from "@/containers/Returns/ReturnsContainer";


export default function Returns() {
  return (
    <Layout>
      <ReturnsContainer />
    </Layout>
  );
}
